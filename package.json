{
  "name": "white-whaling",
  "version": "1.0.0",
  "description": "White Whaling Landing Page and Blog",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "build": "npm install && npm run install:server",
    "install:server": "cd server && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd src && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
